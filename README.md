# CoworkHub

A database-first coworking space management system built with PostgreSQL and Django.

## Overview
The core logic lives in the database — triggers automatically generate and update invoices on reservation insert/cancel. Django serves as a lightweight management interface.

apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: coworkhub
data:
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_NAME: coworking_db
  DB_USER: postgres
  DEBUG: "False"
  ALLOWED_HOSTS: "localhost,127.0.0.1"
  CORS_ALLOWED_ORIGINS: "http://localhost:5173"